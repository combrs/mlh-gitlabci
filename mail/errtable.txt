 E010# Не разрешенный тип JIRA ID ($ISSTYPES) или в JIRA ID после типа не номер (содержит не цифры) :
 E011# Не найден файл readme $COMMIT.txt, невозможно извлечь данные (JIRA ID, описание, др.)
 E012# Не найден файл с именем найденного тикета в Jira DSO, невозможно проверить тикет
 E013# Не найден файл с результатами поиска в Jira DSO, невозможно проверить тикет
 E014# В файле readme $COMMIT.txt совсем не найдена строка с JIRA ID
 E015# Не найдена тема (Title) в файле $COMMIT.txt
 E016# Не найден вендор (Vendor) в файле $COMMIT.txt
 E017# Не найден автор (Author) в файле $COMMIT.txt
 E018# Группа $BRANCH найдена в ansible inventory, но она пуста (0 хостов), некуда устанавливать.
 E019# Конфигурация сервера в группе $BRANCH в ansible (inventory/hosts), не содержит пути к базе (group_vars/somebase.fbddir)
 E020# Не найден файл с переменными для группы в ansible : inventories/group_vars/$BRANCH.yml
 E021# При проверке JIRA ID не найден файл commheader.txt, который должен был быть сформирован из $COMMIT.txt
 E022# Проблема у программы перекодирования файла, входящего в поставку, из неверной кодировки. См. лог.
 E023# Проблема доступа к Jira DSO для получения таблицы соотвествия автора поставки и логина в Jira для назначения задачи
 E024# Проблема доступа к Jira DSO при создании новой заявки, когда заявка для $COMMIT не найдена.
 E025# Проблема доступа к Jira DSO при назначении заявки для указанного аккаунта.
 E026# Не найден релиз (Release) в файле $COMMIT.txt
 E027# Не найден автор в списке пользователей JIRA DSO при создании заявки, поэтому назначена на пользователя pipeline-dso.
E100#Отсутствует файл $COMMIT.txt в nexus каталоге поставки somebase/distribs/develop/$COMMIT/
E101#Дубликат поставки $COMMIT (pipeline #${CI_PIPELINE_ID}(${CI_PIPELINE_SOURCE}), запустил ${GITLAB_USER_NAME} [${GITLAB_USER_EMAIL}] )
 E102# Подключение к nexus успешно,  но поиск в nexus папки somebase/distribs/develop/$COMMIT ничего не дал
 E103# Отсуствует (не создан) файл ST_$COMMIT.zip при попытке заархивировать скачанное в Download_supply
 E104# Невозможно загрузить файл (ошибка в nexus - неверный url, файл не найден).
 E105# Не найден архив поставки ST_$COMMIT.tar.gz после загрузки всего из nexus somebase/distribs/develop/$COMMIT/. Невозможно продолжить.
 E106# Проблемы с распаковкой архива поставки ST_$COMMIT.tar.gz для извлечения файла CR_contents.txt
 E107# Переданное имя ветки $BRANCH не существует в исходном репозитории git (somebasehost)
 E108# Не найден файл с результатами поиска содержимого каталога поставки в nexus (somebase/distribs/develop/$COMMIT/ )
 E109# Невозможно загрузить файл на этапе download_supply, ошибка в сервере nexus отличная от 'не найден'.
 E110# Файл ST_$COMMIT.tar.gz существует в nexus somebase/distribs/develop/$COMMIT/, но не был загружен из-за бага поиска в nexus.
 E111# Не найден файл поставки ST_$COMMIT.tar.gz перед запаковкой поставки в zip.
 E112# Nexus bug: Файл найден в nexus как component, но он не содержит скачиваемых assets (файл удален из nexus, но поиск выдает его)
 E113# После загрузки всего из nexus somebase/distribs/develop/$COMMIT/ файл  ST_$COMMIT.tar.gz не обнаружен на диске, и прямой запрос отвечает, что файл в nexus отсутствует.
 E114# В поставке найдены файлы в неверной кодировке или неверной длины. Приложен их список и (если кодировка) архив zip с файлами, перекодированными в UTF-8
 E115# Вместо файла $COMMIT.txt в nexus somebase/distribs/develop/$COMMIT/ найдены лишние readme файлы :
 E180# В обозначении релиза после # есть не цифры или они не соотвествуют формату #{номер от 1 до 3 цифр}/{год из двух цифр, текущий или следующий}
 E181# В обозначении релиза - начало не MLH_Release# или After_Release#
 E182# В файле $COMMIT.txt после 'Dependencies:' есть строки (зависимости) не формата 'MLH-NNNN'
 E183# В обозначении релиза указан неверный год - не прошлый, нынешний или следующий
 E190# Переданный аргумент номер поставки (после MLH-) не является цифрами: $COMMIT
 E191# Переданный аргумент название поставки некорректен : $COMMIT
 E192# Вызов скрипта nexus-do.sh сделан с неподдерживаемой командой
 E193# Сборка из бранча или тега вида $COMMIT не поддерживается
 E194# Для заданного бранча $BRANCH не существует конфигурации для деплоя в ansible
 E200# Отсутствует файл с результатами разбора файла readme , невозможно получить значение Vendor
 E201# При изменении статуса задачи вызов HTTP REST API привел к ошибке
 E202# При изменении поля CR задачи вызов HTTP REST API привел к ошибке
 E203# При отправке комментария к задаче  вызов HTTP REST API привел к ошибке
 E204# Не найден файл результатов Deploy для записи в таблицу Установлено на
 E205# При записи в таблицу установлено на  вызов HTTP REST API привел к ошибке
 E206# Не запощен комментарий про автотесты потому что они выключены (P_TESTS) или файл подсчитанных результатов отсутствует
 E290# Слишком мало аргументов для вызова скрипта jira-api-search-do.sh
 E291# Вызов скрипта jira-api-search-do.sh сделан с неподдерживаемой командой
 E292# Найдены несоотвествия в JIRA-DSO для задачи
 E293# Переданный аргумент номер поставки (после MLH-) не является цифрами : $COMMIT
E294#Переданный аргумент название поставки некорректен : $COMMIT
 E295# задача в Jira не найдена, но она не создана поскольку Вендор = MLH
 E289# Неуспешный поиск в JiraDSO, HTTP код см. в логе
 E300# Ошибка : не найден лог инсталляции FixPackдля проверки
 E301# Найдены ошибки в логе инсталляции FixPack
 E302# Не найден лог проверки лога инсталляции FixPack
 E303# P_DEPLOY_CHECK=$P_DEPLOY_CHECK, поэтому в таблицу (установлено на) записано об отключении проверки
 E304# Найдены предупреждения в логе инсталляции FixPack
 E400# Найдены проваленные автотесты. Будет отправлено дополнительное письмо с их списком.
 E402# Ошибка при попытке загрузить файл cucumber.xml в nexus somebase/distribs/develop/$COMMIT/.
 E403# Невозможно найти файл json, подготовленный для отправки списка проваленных тестов в почту.
 E404# Невозможно найти файл с числом проведенных тестов и выяснить их статус.
 E500# Не найден файл с темой, вендором и т.п. для составления письма
 E501# HTTP Ошибка сервера при выгрузке файла в nexus somebase/distribs/release/$COMMIT/
 E600# Задача не найдена (404) при попытке поста комментария в JIRA-GLH.
 E601# Ошибка сервера (500) при попытке поста комментария в JIRA-GLH. 
 E602# Отказано в доступе (403) при попытке поста комментария в JIRA-GLH. 
 E603# Другая ошибка (не 403,404,500)  при попытке поста комментария в JIRA-GLH. 
 E604# Ошибка при посылке почты через  http://10.203.92.186:9080 (с письмом для УВИТ или ошибками). Статус в JIRA-DSO не будет обновлен.
 E606# Список ошибок на wiki отличается от сохраненнгого в somebase_ci/mail/errtable.txt
 E607# Не найден файл со статусом задачи в Jira, требуемый для смены статуса
 E609# При составлении письма в УВИТ не найден файл CR_contents.txt для Scope of delivery
 E610# При попытке проверить сформированный data.json не найден он или файл для сравнения
 E611# Файл для отправки: data.json сформировался некорректно (ошибка в version или supplypath или Jira id, см. лог выполнения)
 E612# Не найден файл с результатами деплоя при извлечении информации о стенде и базе установки (в заголовке письма)
 E613# Нет подключения при попытке поста комментария в JIRA-GLH (curl HTTP error 000)
 E620# Не найден файл с письмом об успешной поставке или ошибками
 E621# Не найден файл с письмом об ошибках в Jira DSO
 E622# Не найден файл c письмом со списком проваленных автотестов.
 E623# Ошибка при отправке письма со списком проваленных автотестов.
 E624# Ошибка при посылке почты через mail-somebase (186: 9080) и envsmtp01 (180) . Статус в JIRA-DSO не будет обновлен.
 E625# Ошибка при посылке почты через mail-somebase (186: 9080) и envsmtp01 (180) (с письмом про ошибки в задаче в JIRA-DSO)
 E700# Ошибка при обращении к Gitlab API для получения имени проваленной задачи
 E800# Ошибка при выполнении ansible playbook на этапе Deploy
